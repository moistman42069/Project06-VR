# Baseline captures

Store pre-mod gameplay observations, screenshots and input traces here when they
help compare Project 06 behavior. The original APK itself stays in the local
ignored `original/` directory and is never committed.
